# orga-next Example

This app demonstrates using Org mode files as top-level pages in Next.js with `@orgajs/next`.

## Run locally

From the repository root:

```bash
pnpm install
pnpm run example:dev
```

Then open http://localhost:3000.
